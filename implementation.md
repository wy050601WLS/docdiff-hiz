# 实现思路 · 文件比对专家（DocDiff HIZ）

> 一句话：**解析 → 段落配对 → 字符级 diff → 规则引擎出结论 → 大模型写审阅意见 → 差异问答带引用溯源。**
> 核心取舍：**大模型只做「表达」，不做「判断」**——结论由可复现的规则引擎给出，模型负责把结论说得像人话。

---

## 一、整体架构

```
        ┌──────────────────────────────┐
        │   前端（原生 HTML/CSS/JS）      │
        │  上传 / 统计徽章 / 报告 / 问答   │
        └───────────────┬──────────────┘
                        │ REST (multipart + JSON)
        ┌───────────────▼──────────────┐
        │        FastAPI 应用层         │
        │  /compare /report /chat       │
        │        /health                │
        └───────────────┬──────────────┘
                        │
     ┌──────────────────┼──────────────────┬─────────────────┐
     ▼                  ▼                  ▼                 ▼
┌──────────┐    ┌──────────────┐   ┌──────────────┐  ┌─────────────┐
│ 解析层    │    │  比对层       │   │  报告层       │  │  对话层      │
│ Docling  │    │ 滑动窗口配对  │   │ 规则引擎      │  │ 上下文注入   │
│ ↓ 兜底    │    │ 相似度打分    │   │ ↓ 可选增强    │  │ 引用溯源     │
│ pypdf    │    │ 字符级 diff   │   │ 本地 LLM      │  │ 离线兜底     │
└──────────┘    └──────────────┘   └──────────────┘  └─────────────┘
                                                          │
                                                   ┌──────▼──────┐
                                                   │ 本地 Ollama  │
                                                   │ qwen3.5:9b  │
                                                   └─────────────┘

  会话层（横切）：每次比对建一个会话 → data/sessions/<id>.json
                  存「比对结果 + 报告 + 全部消息（带时间戳）」，原子写入，重启不丢
```

## 二、核心流程（六步）

1. **解析**：Docling 导出 Markdown → 过滤页眉页脚/页码 → 拆成带标题层级的段落。
   Docling 不可用时自动降级 pypdf，并按空行 + 短行合并还原段落边界。
2. **配对**：以旧版段落为基准，在 `[i-5, i+5]` 窗口内找相似度最高的未配对段落。
   记录**所有**配对分数（含未达阈值的），作为后续同源判定的依据。
3. **分类**：`≥0.95 相似` / `≥阈值 修改` / `仅旧版 删除` / `仅新版 新增`。
   修改项生成字符级 diff HTML（`<del>` 红、`<ins>` 绿）。
4. **同源判定**：`无任何相似段落 且 平均配对相似度 < 40%` → 判为异源文档，
   给出相关度而非硬拒绝。（这是参考实现里「建议重新上传」的升级版）
5. **报告**：规则引擎先产出结论、改动率、风险点；本地模型在线时再补一段审阅意见。
6. **问答**：把差异明细压成上下文注入对话，回答里用 `[序号]` 标注来源，前端可回溯。
7. **存档**：比对完成即建会话（比对结果 + 报告 + 消息全部落盘，原子写入），写入失败不影响主流程返回。

## 三、选型理由（面试可展开）

| 决策 | 选择 | 为什么 |
|------|------|--------|
| 比对算法 | difflib + 滑动窗口 | 不引入 embedding 模型，**零额外依赖、零 GPU、结果可复现**。段落顺序在版本演进中基本稳定，滑窗足够；真要做跨章节重排再上向量检索 |
| 大模型定位 | 只写审阅意见，不产出结论 | 结论必须可复现、可审计。让模型做判断会引入幻觉，规则引擎才是兜底（呼应「AI 除幻」思路） |
| 模型接入 | OpenAI 兼容协议 | 本地 Ollama / vLLM / 云端 API 一套代码通吃，切换只改三行配置 |
| Docling 可选化 | 装了用，没装降级 | 它要拉 torch（100MB+）和解析模型。**不能因为一个重依赖让整个项目跑不起来** |
| 前端零构建 | 原生三件套 | 面试作业优先「打开就能跑」，不引入 node_modules 和构建链 |
| 会话存储 | JSON 文件 + 原子写入 | 这个量级零依赖最划算：能直接打开看、拷走即备份；查询就是读目录。量级到了再换 SQLite |
| 本地模型加速 | Ollama 时走原生 `/api/chat` | 关掉思考模式，同类问答从 37.7s 降到 1.8s；失败自动回落 OpenAI 兼容接口，不锁死供应商 |

## 四、踩坑记录（真实发生）

1. **Docling 装不上**：被重型依赖链拖累，安装过程反复失败。
   → 决策：把它改为 optional extra，并补 pypdf 兜底 + 启动时自检。**先保证可用，再谈最优。**
2. **异源判定一开始漏了**：最初用「段落数 > 2 且无相似段落」判断，
   结果单段落的异源文档（如规格书 vs 简历）被误判为「全部修改」。
   → 改为记录**配对相似度均值**做判定，并补了 `test_uncomparable_documents` 回归用例。
3. **字符级 diff 不一定是「替换」**：纯插入句子的改动只有 `diff-ins`，没有 `diff-del`。
   测试断言一开始写死了 `diff-del` 导致失败。→ 断言改为「出现 ins 或 del 即可」。
4. **本地小模型超时**：9B 模型首 token 可能等几十秒。→ 超时放宽到 300s，并做 `is_available()` 预检，
   不在线就不进 LLM 分支，避免用户干等。
5. **测试依赖网络**：报告测试最初会去探测 Ollama，导致测试挂住。
   → `generate_report(use_llm=False)` 参数化，测试与网络解耦，8 个用例 0.5s 跑完。
6. **思考型模型把预算全花在「想」上**：qwen3.5 走 OpenAI 兼容接口时，`max_tokens` 会把思考 token
   一起算进去，512 的额度全被思考块吃掉，正文被截断在半句话；一次问答实测 **37.7s**。
   → 检测到本机 Ollama 时改走原生 `/api/chat` 并 `think=false`，同样的问答降到 **1.8s**（约 -95%），
   原生接口不支持时自动回落兼容接口。**这条是「同一件事换条路走，快 20 倍」的典型例子。**
7. **界面上「发了没反应」**：其实是在等模型——非流式要等整段生成完才返回，等待期间前端没有任何反馈。
   → 前端补「正在思考中…」占位气泡，对话前再做一次 3 秒快速探活，模型不在线就秒回规则检索结果。
8. **会话持久化的两个坑**：① FastAPI 同步路由跑在线程池里，两个请求同时写同一会话会互相覆盖
   → 加线程锁；② 进程中途被杀会写出半个 JSON → 临时文件 + `os.replace` 原子替换，
   最坏只丢「正在写的那一条」，已提交的记录永远完整。
9. **会话列表的排序会「抖」**：`updated_at` 一开始只精确到秒，「同一秒内建 A、建 B、再更新 B」
   三件事撞在一起，排序键全部相等 → 顺序退化成随机，界面表现为「刚操作过的会话没排到最前」。
   这是跑回归时偶然撞出来的。→ 时间戳改成微秒级，排序再加 `session_id` 兜底，
   并补了「连取 5 次顺序必须一致」的回归用例。**注意这类问题单测很容易漏，靠的是反复跑。**

## 五、超出参考实现的地方

| 维度 | 参考实现 | 本实现 |
|------|---------|--------|
| 比对粒度 | 结构级 / 段落级汇总 | **段落级配对 + 字符级 diff 高亮** |
| 结论生成 | 完全交给 LLM | **规则引擎出结论 + LLM 润色**，可复现、可审计 |
| 异源处理 | 判「没有可比性」并让重传 | 给出**相关度百分比**与具体建议 |
| 对话 | 基于比对结果回答 | 额外做 **`[序号]` 引用溯源** |
| 会话留痕 | — | **每次比对自动存档**，消息带时间戳，可搜索 / 恢复 / 删除，重启不丢 |
| 可用性 | 依赖模型在线 | **模型不在线自动降级**，报告/问答都不阻塞 |
| 交付质量 | — | 单元测试 16 项、4 个自检脚本、离线可点 Demo、示例 PDF、一键启动脚本 |

### 关于前端的两点补充

**1. 前端不是「附赠的界面」，它承担了差异的可读性。** 段落级 diff 的结果如果只是 JSON，面试官和业务方都看不出价值；所以前端把四类差异做成了统计徽章，报告里修改项额外渲染**字符级红删绿增**，对话回答里带 **`[n]` 引用**可以回指到报告的具体差异序号。可读性做出来，规则引擎 + LLM 两段式的价值才看得见。

**2. 前端做了离线可点的 Demo（`samples/ui-demo.html`）。** 演示现场最怕的是「网络不通 / 依赖没装 / 模型没起」，所以把 `diff_engine.py` 的算法原样移植了一份到浏览器（LCS 相似度 + 滑动窗口配对 + 字符级 diff），内联在单文件 HTML 里，双击就能走完整流程，结果实时算出来而非写死。Demo 里内置两套示例场景（技术规格说明书、采购合同），侧边栏「审核历史」用的是浏览器本地存储，会话带时间戳、可搜索、可恢复、可删除——**功能是真的，数据是自己的**；如果浏览器禁用了本地存储，界面会直接写明「历史仅本次有效」，不假装持久化成功。

这里有个刻意的设计取舍：**同一套算法写了两遍（Python + JS）**，看起来违反 DRY。理由是演示可靠性的优先级高于代码复用——为了让它能被任何一台没装环境的电脑打开，这个重复是值得的。为此补了两个自检脚本：`verify_demo.cjs` 从 HTML 里抽出内联脚本，在 node 里把两套场景各跑一遍，校验统计与报告结构；`verify_demo_ui.cjs` 用 jsdom 真实点击，走完「比对 → 问答 → 历史 → 恢复 → 删除」并检查会话是否真的落盘。保证两份实现不会悄悄跑偏。真要做成产品，正确做法是只保留 Python 一份，前端纯调 API——**面试时这段可以直接讲「我知道这是重复，也知道为什么现在接受它」**。

## 六、如果给一周怎么扩展

1. **多格式**：Word / TXT 输入（docling 已原生支持，只需放开文件类型白名单）。
2. **跨章节匹配**：段落顺序大挪移时，用向量相似度做全局最优匹配（bge-m3 + 匈牙利算法）。
3. **原文定位**：差异项回填页码 + 段落锚点，前端点击跳转原文位置。
4. **表格与条款级比对**：按表格行列、按「第 X 条」结构化切分，做字段级 diff，而非整段对比。
5. **观测与评测**：接 Langfuse 记录 LLM 调用链路，沉淀 Bad Case 并做 LLM-as-a-Judge 评测。
6. **工作流化**：比对 → 人工复核 → 生成审批单 → 归档，接进现有 OA / 审批系统。
7. **性能**：解析与比对放后台任务队列（Celery/RQ），大文档不阻塞请求。

## 七、业务场景对号入座

面试官原话讲的是「TOB 里会有很多文档审阅场景，两个文档进行对比」。
这套流程可直接复用到：

- **铜贸情报报告**：昨日版 vs 今日版，自动标出数据更新、观点变化、风险新增点。
- **标书 / 合同版本审查**：识别条款删改、金额调整、交付期变更，规则引擎自动提示法务关注项。
- **技术规格书评审**：硬件参数、软件版本、性能指标的逐项变更追踪。
