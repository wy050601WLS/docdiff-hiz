# 实现思路

链路是解析、段落配对、字符级 diff、规则引擎出结论、大模型写审阅意见、差异问答带引用溯源。

核心取舍只有一条，大模型只做表达，不做判断。结论由可复现的规则引擎给出，模型负责把结论说得像人话。

## 整体架构

```
        ┌──────────────────────────────┐
        │   前端（原生 HTML/CSS/JS）      │
        │  上传 / 统计徽章 / 报告 / 问答   │
        └───────────────┬──────────────┘
                        │ REST (multipart + JSON)
        ┌───────────────▼──────────────┐
        │        FastAPI 应用层         │
        │  /compare /report /chat       │
        │        /health                │
        └───────────────┬──────────────┘
                        │
     ┌──────────────────┼──────────────────┬─────────────────┐
     ▼                  ▼                  ▼                 ▼
┌──────────┐    ┌──────────────┐   ┌──────────────┐  ┌─────────────┐
│ 解析层    │    │  比对层       │   │  报告层       │  │  对话层      │
│ Docling  │    │ 滑动窗口配对  │   │ 规则引擎      │  │ 上下文注入   │
│ ↓ 兜底    │    │ 相似度打分    │   │ ↓ 可选增强    │  │ 引用溯源     │
│ pypdf    │    │ 字符级 diff   │   │ 本地 LLM      │  │ 离线兜底     │
└──────────┘    └──────────────┘   └──────────────┘  └─────────────┘
                                                          │
                                                   ┌──────▼──────┐
                                                   │ 本地 Ollama  │
                                                   │ qwen3.5:9b  │
                                                   └─────────────┘
```

会话层是横切的一条。每次比对建一个会话，落成 data/sessions 下的一个 JSON，存比对结果、报告和全部消息，消息带时间戳，原子写入，重启不丢。

## 核心流程

1. 解析。Docling 导出 Markdown，过滤页眉页脚和页码，拆成带标题层级的段落。Docling 不可用时自动降级 pypdf，按空行加短行合并还原段落边界。
2. 配对。以旧版段落为基准，在前后各 5 段的窗口内找相似度最高的未配对段落。所有配对分数都记录，包括没达到阈值的，后面同源判定要用。
3. 分类。相似度 0.95 以上算相似，达到阈值算修改，只在旧版出现的算删除，只在新版出现的算新增。修改项会生成字符级 diff HTML。
4. 同源判定。没有任何相似段落，且平均配对相似度低于 0.4，判为异源文档，给出相关度而不是直接拒绝。这是参考实现里建议让用户重新上传的升级版。
5. 报告。规则引擎先出结论、改动率、风险点。本地模型在线时再补一段审阅意见。
6. 问答。差异明细压成上下文注入对话，回答里用序号标注来源，前端可以回溯。
7. 存档。比对完成就建会话，比对结果、报告、消息全部落盘。写入失败不影响主流程返回。

## 为什么这么选

比对算法用 difflib 加滑动窗口。不引入 embedding 模型意味着零额外依赖、零 GPU、结果可复现。段落顺序在版本演进里基本稳定，滑窗够用。真要做跨章节重排再上向量检索。

大模型只写审阅意见，不产出结论。结论得可复现、可审计，让模型做判断会引入幻觉，规则引擎才是兜底。

模型接入走 OpenAI 兼容协议。本地 Ollama、vLLM、云端 API 一套代码通吃，切换只改三行配置。

Docling 做成可选依赖。它要拉 torch 和一整套解析模型，不能因为一个重依赖让整个项目跑不起来。装了就用，没装就降级。

前端零构建，原生三件套。面试作业优先保证打开就能跑，不引入 node_modules 和构建链。

会话存储用 JSON 文件加原子写入。这个量级零依赖最划算，能直接打开看，拷走就是备份，查询就是读目录。量级上来了再换 SQLite。

指向本机 Ollama 时走原生 /api/chat。关掉思考模式，同类问答从 37.7 秒降到 1.8 秒。失败自动回落 OpenAI 兼容接口，不锁死供应商。

## 踩坑记录

1. Docling 装不上。被重型依赖链拖累，安装反复失败。改成 optional extra，补 pypdf 兜底和启动自检。先保证可用，再谈最优。

2. 异源判定一开始漏了。最初用段落数大于 2 且无相似段落来判断，结果单段落的异源文档（比如规格书对简历）被误判成全部修改。改成记录配对相似度均值来判定，补了 test_uncomparable_documents 回归用例。

3. 字符级 diff 不一定是替换。纯插入句子的改动只会有 diff-ins，没有 diff-del，测试断言一开始写死 diff-del 就挂了。断言改成出现 ins 或 del 即可。

4. 本地小模型超时。9B 模型首 token 可能等几十秒。超时放宽到 300 秒，并且做 is_available() 预检，不在线就不进 LLM 分支，避免用户干等。

5. 测试依赖网络。报告测试最初会去探测 Ollama，导致测试挂住。给 generate_report 加了 use_llm 参数，测试与网络解耦，8 个用例 0.5 秒跑完。

6. 思考型模型把预算全花在想上。qwen3.5 走 OpenAI 兼容接口时 max_tokens 会把思考 token 一起算进去，512 的额度全被思考块吃掉，正文截断在半句话，一次问答实测 37.7 秒。检测到本机 Ollama 时改走原生 /api/chat 并设 think=false，同样的问答降到 1.8 秒，差不多快 20 倍。原生接口不支持时自动回落兼容接口。

7. 界面上看起来发了没反应。其实是在等模型，非流式要等整段生成完才返回，等待期间前端没有任何反馈。前端补了正在思考中的占位气泡，对话前再做一次 3 秒快速探活，模型不在线就秒回规则检索结果。

8. 会话持久化的两个坑。一是 FastAPI 同步路由跑在线程池里，两个请求同时写同一会话会互相覆盖，加了线程锁。二是进程中途被杀会写出半个 JSON，改成临时文件加 os.replace 原子替换，最坏只丢正在写的那一条，已提交的记录永远完整。

9. 会话列表的排序会抖。updated_at 一开始只精确到秒，同一秒内建 A、建 B、再更新 B 这三件事撞在一起，排序键全部相等，顺序退化成随机，界面上表现为刚操作过的会话没排到最前。这是跑回归时偶然撞出来的。时间戳改成微秒级，排序再加 session_id 兜底，补了连取 5 次顺序必须一致的回归用例。这类问题单测很容易漏，靠的是反复跑。

## 比参考实现多做的部分

段落级配对加字符级 diff 高亮，参考实现只到结构级或段落级汇总。结论由规则引擎出、LLM 润色，不是完全交给模型，可复现可审计。异源文档给相关度百分比和具体建议，而不是判没有可比性就让重传。对话额外做了序号引用溯源。每次比对自动存档，消息带时间戳，可搜索、恢复、删除，重启不丢。模型不在线自动降级，报告和问答都不阻塞。交付上还有单元测试 16 项、4 个自检脚本、离线可点 Demo、示例 PDF、一键启动脚本。

### 关于前端

前端不是附赠的界面，它承担了差异的可读性。段落级 diff 的结果如果只是 JSON，面试官和业务方都看不出价值。所以前端把四类差异做成了统计徽章，报告里修改项额外渲染字符级红删绿增，对话回答里带 [n] 引用可以回指到报告的具体差异序号。可读性做出来，规则引擎加 LLM 两段式的价值才看得见。

前端还做了一份离线可点的 Demo，就是 samples/ui-demo.html。演示现场最怕网络不通、依赖没装、模型没起，所以把 diff_engine.py 的算法原样移植了一份到浏览器，LCS 相似度加滑动窗口配对加字符级 diff，内联在单文件 HTML 里，双击就能走完整流程，结果实时算出来而不是写死。Demo 里内置两套示例场景，侧边栏的审核历史用的是浏览器本地存储，会话带时间戳，可搜索、可恢复、可删除。如果浏览器禁用了本地存储，界面会直接提示历史仅本次有效，不假装持久化成功。

这里有个刻意的重复，同一套算法写了两遍，Python 一份、JS 一份，看起来违反 DRY。理由是演示可靠性的优先级高于代码复用，为了让它能被任何一台没装环境的电脑打开，这个重复是值得的。为此补了两个自检脚本。verify_demo.cjs 从 HTML 里抽出内联脚本，在 node 里把两套场景各跑一遍，校验统计与报告结构。verify_demo_ui.cjs 用 jsdom 真实点击，走完比对、问答、历史、恢复、删除，并检查会话是否真的落盘。这样才能保证两份实现不会悄悄跑偏。真要做成产品，正确做法是只保留 Python 一份，前端纯调 API。

## 如果给一周

1. 多格式支持。Word 和 TXT 输入，docling 已经原生支持，只需要放开文件类型白名单。
2. 跨章节匹配。段落顺序大挪移时，用向量相似度做全局最优匹配，bge-m3 加匈牙利算法。
3. 原文定位。差异项回填页码和段落锚点，前端点击跳转原文位置。
4. 表格与条款级比对。按表格行列、按第 X 条结构化切分，做字段级 diff，而不是整段对比。
5. 观测与评测。接 Langfuse 记录 LLM 调用链路，沉淀 Bad Case，做 LLM-as-a-Judge 评测。
6. 工作流化。比对、人工复核、生成审批单、归档，接进现有 OA 或审批系统。
7. 性能。解析与比对放后台任务队列，Celery 或 RQ，大文档不阻塞请求。

## 业务场景

面试官原话讲的是 TOB 里会有很多文档审阅场景，两个文档进行对比。这套流程可以直接复用到几个地方。

铜贸情报报告，昨日版对今日版，自动标出数据更新、观点变化、风险新增点。

标书或合同版本审查，识别条款删改、金额调整、交付期变更，规则引擎自动提示法务关注项。

技术规格书评审，硬件参数、软件版本、性能指标的逐项变更追踪。
