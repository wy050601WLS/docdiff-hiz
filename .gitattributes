# 统一换行符：仓库内一律 LF，避免 Windows / macOS 协作出噪音
* text=auto eol=lf

# Windows 批处理必须是 CRLF，否则 cmd 解析可能出错
*.bat text eol=crlf
*.cmd text eol=crlf

# 二进制文件不做换行转换
*.pdf binary
*.png binary
*.jpg binary
*.ico binary
*.woff binary
*.woff2 binary
