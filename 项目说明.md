# DocDiff HIZ 项目说明

代码在 https://github.com/wy050601WLS/docdiff-hiz

两份同一文档的新旧 PDF 传上去，机器把差异算出来：段落怎么配的对、哪句话改了哪几个字、整体的审阅结论是什么，之后可以针对某一条差异继续追问。模型和解析器都不在线也能跑完。

后端是 FastAPI，前端走 REST，不是纯前端。跑起来有两种方式。

Docker，应用和本地大模型一起编排（推荐，不用在本机装 Python 和 Ollama）：

```bash
git clone https://github.com/wy050601WLS/docdiff-hiz.git
cd docdiff-hiz
docker compose up -d --build
docker compose exec ollama ollama pull qwen3.5:9b   # 首次拉模型
```

本地直接跑：

```bash
python -m venv .venv && .venv\Scripts\python.exe -m pip install -e ".[dev]"
.venv\Scripts\python.exe -m uvicorn docdiff.main:app --app-dir src --port 8000
```

两种方式都开 http://127.0.0.1:8000。Windows 上依赖装好后双击 `run.bat` 也行。不用配 `.env`，配置都有默认值，默认指向本机 Ollama；没装 Ollama 服务也起得来，报告会退成规则引擎版。

`samples/ui-demo.html` 是给没环境时看界面用的单文件离线 Demo，双击就能点完整流程，它不代表系统架构，正式版是前后端分离的。

---

## 一、要解决什么

ToB 里的文档审阅场景不少，合同版本审查、技术规格书评审这类的，本质上都是拿两份文档比对。做的时候给自己定了三条线：

1. 改动要落到字上，不能停在"这一段变了"就完事。
2. 结论由代码算，可复现、能追溯。让模型直接下判断会飘，出了问题没法追到具体规则。
3. 模型挂了不等于系统挂了，比对、报告、问答这三样离线必须还能跑。

---

## 二、功能

| 功能 | 实现情况 |
|------|----------|
| 双 PDF 上传 | 点击或拖拽都行，multipart 提交，解析完删临时文件 |
| 段落级配对 | 滑动窗口加相似度打分，分出一致 / 修改 / 新增 / 删除四类 |
| 字符级 diff | 修改段落逐字比对，红删绿增高亮 |
| 原文对照与定位 | 每个差异项标出新旧版所在页码，双栏对回原文；问答里的 `[序号]` 点一下跳到该条并高亮闪烁 |
| 结构化报告 | 综合结论、规则统计、规则分析、逐条差异明细，HTML 和 Markdown 都出 |
| LLM 审阅意见 | 在规则结论之上补一段 200 字左右的意见，说风险在哪，不复述改动清单 |
| 差异问答 | 回答带 `[序号]`，能对回具体的差异项 |
| 会话存档 | 每次比对自动落一个会话，结果、报告、全部对话存盘，可检索、恢复、删除，重启不丢 |
| 异源兜底 | 两份文档没关系时给相关度和处理建议，不硬凑结论 |
| 降级 | 解析器和模型任一不可用，主流程照样走完 |

---

## 三、实现

整体是四层加一条横切的会话层。

```
前端（原生 HTML/CSS/JS，无构建）
        │  REST
FastAPI    /compare  /report  /chat  /health  /sessions
        │
   解析层    比对层    报告层    对话层
        │
会话层：每次比对写一个 data/sessions/{id}.json
```

### 解析这块

Docling 优先，导出 Markdown 并保留标题层级，装不上就退到 pypdf。pypdf 这一路是逐页解析的，页码记在每个段落上，差异项才能回指到原文的哪一页。Docling 那一路优先用 `iterate_items` 逐块取文本，同样带页码，接口对不上就退回 Markdown 导出件，功能不受影响，只是页码拿不到，前端就不显示定位。pypdf 拿不到结构化信息，只能靠两件事往回补：过滤噪音（长度小于 6 的、纯页码和分隔符行丢掉），再做短行合并，少于 20 字且不以句号分号冒号结尾的行并到上一行，尽量还原真实的段落边界。标题层级是拿"长度小于 40 且匹配 `第X章` 或数字编号"粗判的，这块比较糙，能用但没有很靠谱。

### 比对怎么算的

相似度用的是 `difflib.SequenceMatcher` 的字符级 ratio，算之前先去空白转小写。配对以旧版为基准，在前后各 5 段的窗口里找相似度最高的未配对段落，达到 0.6 才算配上。

- 0.95 以上算一致
- 0.6 到 0.95 之间算修改
- 只有旧版有的算删除，只有新版有的算新增

修改项额外生成字符级 diff 的 HTML，`replace` 会拆成 delete 加 insert 两段。

同源判定这块，所有配对候选的分数都记下来了，包括没过阈值的，这是关键。命中"一致数为 0 且平均分低于 0.4"就判异源，返回主题相关度而不是假装它是版本差异。

风险分级比较简单：增删数多于修改加一致数算高，修改数多于一致数算中，其余低。

### 报告分两段出

规则引擎先离线出结论，包括改动率、风险点（10 个关键词命中，外加超过 80 字的大段删除检测，都带差异序号）、删比增多时的补漏提示。模型在线再在规则结论之上补一段表达层的审阅意见，调用失败直接降级，不往外抛错。

Markdown 转 HTML 是自己写的极简实现，只处理标题、列表、加粗、段落，够用就没引 markdown 库。

### 问答

差异明细压缩成上下文塞给模型，限 40 条、每条截断 200 字，历史消息只留最近 6 条，都是怕撑爆本地小模型的上下文窗口。提示词要求用 `[序号]` 标来源，后端拿正则抽出来放进 `references` 字段返回。模型不可用时退化成按中文二元组关键词去检索差异清单，命中哪条返回哪条。

### 模型接入

统一走 OpenAI 兼容协议，本地 Ollama、vLLM、云端 API 之间切换只改环境变量。检测到 base_url 指向本机 Ollama 时会自动改走原生 `/api/chat` 并关掉思考模式，原因是这样的：qwen3.5 这类思考型模型走兼容接口时，思考 token 也算进 `max_tokens`，512 的额度全被思考块吃掉，正文截断在半句，实测一次问答 37.7 秒；换原生接口关掉思考后降到 1.8 秒。原生接口是 Ollama 私有格式，所以保留了兼容接口作为回落路径，不绑死供应商。输出统一剥掉 `<think>` 块。

### 会话存档

每会话一个 JSON 文件。这里撞到三个坑：同步路由跑在线程池里，并发写同一会话会互相覆盖，所以加了全局锁；进程中途被杀会写出半个 JSON，改成临时文件加 `os.replace` 原子替换，最坏只丢正在写的那一条，已提交的永远完整；session_id 做 `[0-9a-f]{32}` 正则校验，防路径穿越。

列表按更新时间加 session_id 双键倒序。坏掉的 JSON 直接跳过，不让一个坏文件拖死整个列表。所有写入失败都吞掉，不影响比对结果正常返回。

### 前端

原生三件套，FastAPI 直接托管，没有构建步骤。三个视图：上传、结果概览（统计徽章加结论加问答）、详细报告。

另外做了一份 `samples/ui-demo.html`，单文件离线 Demo。演示的时候最怕网络不通、依赖没装、模型没起，所以把比对算法照 Python 版原样移植了一份 JS 内联进去，双击就能走完整流程，结果是实时算出来的，不是写死的假数据。内置两套示例场景，侧边栏历史用浏览器本地存储，搜索、恢复、删除都能用。浏览器禁用了本地存储的话，界面会直接提示历史仅本次有效，不假装存成功了。

同一套算法写了两遍，确实违反 DRY。这里我的取舍是演示可靠性高于复用。为了防止两份实现悄悄跑偏，配了两个自检脚本，`verify_demo.cjs` 从 HTML 里抽内联脚本在 Node 里把两套场景各跑一遍校验统计和报告，`verify_demo_ui.cjs` 用 jsdom 真点一遍比对、问答、历史、恢复、删除，并检查会话有没有真的落盘。做成产品的话肯定是只留 Python 一份，前端纯调接口。

### 几个选型上的取舍

比对没上向量检索，用 difflib 加滑窗。版本演进里段落顺序基本是稳的，滑窗够用，好处是零额外依赖、不要 GPU、结果可复现。真要处理跨章节大范围重排，再上 embedding 做全局最优匹配也不迟。

Docling 做成 optional extra。它要拉 torch 和一整套解析模型，不能因为一个重依赖导致整个项目跑不起来，所以装了就用，没装降级。

会话存 JSON 没用 SQLite，这个量级下零依赖最划算，能直接打开看，拷走就是备份，量级上来了再换。

前端零构建，优先保证打开就能跑。

---

## 四、踩过的坑

当时一条条改过来，记几个印象深的。

**异源文档被当成全部修改。** 一开始判"段落数大于 2 且无相似段落"，结果规格书对简历这种单段落异源文档直接漏判，全部输出成修改。改成记录配对相似度均值来判定，补了 `test_uncomparable_documents` 回归用例。

**思考型模型把预算全花在想上。** 上面模型接入那节说过，512 的额度被思考块吃掉，正文截断在半句，一次问答 37.7 秒；走原生接口关掉思考后 1.8 秒。数值是本机实测的。

**点了没反应。** 其实是卡在等模型，非流式要整段生成完才返回，等待期间前端没有任何反馈。补了"正在思考中"的占位气泡，对话前再单独做一次 3 秒快速探活，不在线直接秒回规则检索结果。

**并发写会话互相覆盖。** 加锁，具体见上面会话那节。

**会话列表排序会抖。** `updated_at` 原先只精确到秒，"建 A、建 B、再更新 B"这三件事撞在同一秒时排序键全等，顺序退化成随机，界面上的表现是刚操作过的会话没排到最前面。这个 bug 是跑回归时偶然撞出来的，改成微秒级时间戳，排序再加 session_id 兜底，补了"连取 5 次顺序必须一致"的用例。这类问题单测很容易漏，主要还是靠反复跑。

**Docling 装不上。** 安装反复失败，被重型依赖链拖累。改成可选依赖加 pypdf 兜底，再加一个 `/api/health` 做启动自检。先保证可用，再谈最优。

**字符级 diff 不一定是替换。** 纯插入句子的改动只会有 ins 没有 del，测试断言写死 `diff-del` 就挂了，改成出现 ins 或 del 即可。

**测试依赖网络。** 报告测试最初会去探 Ollama，导致测试挂住。给 `generate_report` 加了 `use_llm` 参数，测试就和网络解耦了，8 个用例 0.5 秒跑完。

---

## 五、验证

```bash
pytest -v                                  # 16 项单元测试
python scripts/verify_pipeline.py          # 后端全链路，不起服务
python scripts/verify_history_api.py       # 会话持久化，进程内 TestClient
node scripts/verify_demo.cjs               # Demo 引擎，两套场景校验统计与报告
node scripts/verify_demo_ui.cjs            # Demo 交互，jsdom 点完整流程
```

单元测试覆盖段落相似、修改、新增删除的识别，同源版本对不误判，异源文档兜底，离线规则报告的结构，以及会话的创建、追加、检索、删除、非法 ID 拒绝、列表排序稳定性。

`verify_demo_ui.cjs` 依赖 jsdom，没随项目安装，需要时在项目外单独装，运行时把 `NODE_PATH` 指到对应的 node_modules 上。只跑 `verify_demo.cjs` 的话不需要任何依赖。

---

## 六、成果

链路是通的：传两份 PDF，解析成段落，段落配对成四类，修改项做到字符级高亮，规则引擎出结论，本地模型补一段审阅意见，之后可以针对差异继续追问，全程自动存档。

部署：`docker compose up -d --build` 起全套，应用和 Ollama 一起编排，会话存档挂卷持久化，容器重建不丢；也可以本机 uvicorn 直接跑。另外有 `samples/ui-demo.html` 单文件离线 Demo，双击就能点完整流程，用于没环境时看界面，不代表系统架构。

本次按评审意见补的两块：Docker 编排部署，以及原文对照与定位（差异项标页码、双栏对回原文、问答引用可点跳转）。

交付的东西：源码、Dockerfile 与 docker-compose、16 项单元测试、4 个自检脚本、示例 PDF（技术规格书 Rev01 和 Rev03）、后端真跑出来的样例报告、一键启动脚本、`.env.example`、README 和交付说明。

---

## 七、还不够的地方

解析层：本机实际跑的是 pypdf 兜底那条路，Docling 那支代码写了但我没真正验证过效果，标题层级和表格还原不敢替它保证。扫描件和图片型 PDF 处理不了，没接 OCR。

比对层：0.95 和 0.6 这两个阈值是我拍的经验值，没有标注数据做调参，准确率和召回率算不出来，这是我最不放心的一块。窗口只有正负 5，跨章节搬家会失配成一删一增。表格也没单独处理，当普通文本逐行比，单元格一错位整段就判成修改。

报告层：模型写的审阅意见没有评测机制，好坏只能靠人看，也没沉淀 Bad Case。风险关键词表里那些词是我自己列的，没对齐过真实的审查规范。

工程层：会话存 JSON 文件，单机单实例够用，多实例部署会有写冲突。没有鉴权，没有上传大小和页数的硬限制，大文档解析会阻塞请求，因为没上任务队列。

---

## 八、想请您判断的几件事

1. 比对粒度上，是偏不漏还是偏不吵。我现在偏召回，宁可多报也不漏改动，代价是偶尔有噪声。如果业务更看重报告干净，判定策略要反过来调。
2. 实际场景里扫描件占多大比例。如果多数是扫描件和图片 PDF，我下一步该补 OCR，而不是继续抠文本比对。
3. 报告是终点还是中间产物。现在是给人看的 HTML 和 Markdown，下游如果要接系统（落库、触发审批流），我得把差异结果抽成一份稳定的结构化 schema。
4. 大模型的边界您更认哪种。我刻意只让它做表达。模型自主判断，和规则兜底加模型表达，你们生产里走的是哪条。
5. 有没有我漏掉的场景。比如一份文档要同时对比多个历史版本，或者比对结果需要回流人工确认，以及权限与审计留痕这类硬要求。

---

这个可以吗，有哪里不行，需要改进或哪里有所遗漏的？
