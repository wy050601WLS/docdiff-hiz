# 文档比对系统 交付说明

按您演示的那个形态做完了，上传两份 PDF、自动比对差异、能接大模型对话这条链路都跑通了。想请您看看方向对不对，有没有我漏掉的地方。

代码在 https://github.com/wy050601WLS/docdiff-hiz

跑起来要三步，克隆、装依赖、起服务：

```bash
git clone https://github.com/wy050601WLS/docdiff-hiz.git
cd docdiff-hiz
python -m venv .venv && .venv\Scripts\python.exe -m pip install -e ".[dev]"
.venv\Scripts\python.exe -m uvicorn docdiff.main:app --app-dir src --port 8000
```

Windows 上也可以直接双击 run.bat，依赖装好之后跑第二次就行。

然后打开 http://127.0.0.1:8000 。不用配 .env，配置项都写了默认值，默认指向本机 Ollama。没装 Ollama 也跑得起来，报告会降级成规则引擎版本，只是审阅意见变成模板。要改配置就照 .env.example 建一个。

不想装环境的话，把仓库里 samples/ui-demo.html 下载下来双击就行。单文件离线 Demo，整个流程都能点，内置两套示例场景和会话历史，不依赖后端、网络和模型。

三分钟能看完的路径是这样。先打开 Demo，一键载入示例文档，点开始对比，看统计徽章和综合结论。再点查看详细报告，看修改处的字符级红删绿增。然后问一句金额有什么变化。最后点侧边栏的审核历史看会话留痕。

## 一、做出来的东西

上传两份 PDF，解析成段落，段落之间配对成一致、修改、新增、删除四类，修改项再做到字符级高亮，规则引擎给出结论，本地大模型补一段审阅意见，之后可以针对差异继续追问。

其中三件事我自己比较在意。

一是改动看得见。不只告诉你这一段改了，而是把改动的每个字标出来，红删绿增。

二是结论不由模型下。改动率、风险等级、逐条差异清单都是代码算出来的，模型只负责把结论写成一段人话。结论要可复现、出了问题能追，让模型下判断会飘。

三是模型不在线也能用。报告和问答会自动降级到规则引擎版本，主流程不阻塞。

另外加了会话历史。每次比对自动存档，每条消息带时间戳，服务重启不丢，可以搜索、恢复、删除。

验证过的部分。单元测试 16 项全过。scripts 下有四份自检脚本，能一键复现完整链路，分别是后端链路 verify_pipeline.py、会话持久化 verify_history_api.py、离线 Demo 的比对引擎 verify_demo.cjs、离线 Demo 的交互流程 verify_demo_ui.cjs。

## 二、几个刻意的取舍

比对用 difflib 加滑动窗口，没上向量检索。版本演进里段落顺序基本是稳定的，滑窗够用，而且零额外依赖、结果可复现。真要处理跨章节大范围重排，再上向量也不迟。

大模型只做表达，不做判断，理由和上面第二条一样。

Docling 设成了可选依赖，装不上就自动走 pypdf。它要拉 torch 和一整套解析模型，不能因为它让整个项目跑不起来。

前端零构建，原生三件套，打开就能跑。

## 三、我自己觉得还不够的地方

解析层。Docling 在本机装了几轮都失败，依赖链太重，当前实际跑的是 pypdf 兜底路径。结构化解析的代码写了，但我没真正验证过效果，标题层级和表格还原我不敢替它保证。扫描件和图片型 PDF 也处理不了，没有 OCR 兜底。

比对层。阈值 0.95 和 0.6 是我拍的经验值，没有标注数据做调参，算不出准确率和召回率，这是我最不放心的一块。段落被大范围重排、跨章节搬家会失配，判成一删一增，因为窗口只有正负 5。表格也没有单独处理，被当普通文本逐行比，单元格一错位整段就判成修改。

报告层。模型写的审阅意见没有评测机制，好坏只能靠人看，也没沉淀 Bad Case。风险关键词表里那些词是我自己列的，没对齐过真实审查规范。

工程层。会话存 JSON 文件，单机单实例够用，多实例部署会有写冲突。没有鉴权，也没有上传大小和页数的硬限制，大文档解析会阻塞请求，因为没上任务队列。

## 四、想请您帮忙判断的几件事

1. 比对粒度上，是偏不漏还是偏不吵。我现在偏召回，宁可多报也不漏改动，代价是偶尔有噪声。如果业务上更看重报告干净，判定策略要反过来调。

2. 实际场景里扫描件占多大比例。如果多数是扫描件和图片 PDF，我下一步该补 OCR，而不是继续优化文本比对。

3. 报告是终点还是中间产物。现在是给人看的 HTML 和 Markdown。如果下游要接系统，比如落库、触发审批流，我得把差异结果抽成一份稳定的结构化 schema。

4. 大模型的边界您更认哪种。我刻意只让它做表达。模型自主判断，和规则兜底加模型表达，你们生产里走的是哪条。

5. 有没有我漏掉的场景。比如一份文档要同时对比多个历史版本、比对结果需要回流人工确认，或者权限与审计留痕这类硬要求。

---

就这些。哪里方向不对，或者有更该优先做的，麻烦您直接说，我按您的判断改。
