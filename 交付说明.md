# 文档比对系统 · 交付说明

**先说结论**：按您演示的那个形态做完了 —— 上传两份 PDF、自动比对差异、能接大模型对话。想请您看下方向对不对，以及有没有我漏掉的地方。

**代码**：https://github.com/wy050601WLS/docdiff-hiz

**跑起来**（克隆 → 装依赖 → 起服务）：

```bash
git clone https://github.com/wy050601WLS/docdiff-hiz.git
cd docdiff-hiz
python -m venv .venv && .venv\Scripts\python.exe -m pip install -e ".[dev]"
.venv\Scripts\python.exe -m uvicorn docdiff.main:app --app-dir src --port 8000
```

Windows 下也可以直接双击 `run.bat`，依赖装好后再跑一次就行。

浏览器打开 http://127.0.0.1:8000 。**不用配 `.env`**，配置项都有默认值，默认指向本机 Ollama；**没装 Ollama 也不影响跑通**（自动降级到规则引擎，只是审阅意见变模板）。要改就照 `.env.example` 建一个。

**不想装环境**：直接把仓库里的 `samples/ui-demo.html` 下载下来双击，单文件离线 Demo，全流程可点（含两套示例场景和会话历史），不依赖后端、网络和模型。

**3 分钟看什么**：打开 Demo → 一键载入示例文档 → 开始对比 → 看统计与结论 → 点「查看详细报告」看字符级红删绿增 → 问一句「金额有什么变化」→ 侧边栏「审核历史」看会话留痕。

---

## 一、做出来的东西

链路：上传两份 PDF → 解析成段落 → 段落配对（一致 / 修改 / 新增 / 删除）→ 改动处字符级高亮 → 规则引擎出结论 → 本地大模型补一段审阅意见 → 可针对差异继续追问

三个我自己比较在意的点：

1. **改动看得见**：不只告诉你「这一段改了」，而是把改的每个字标出来（红删绿增）。
2. **结论不由模型下**：改动率、风险等级、逐条差异清单都是代码算的，模型只负责把结论写成一段人话。结论要可复现、出了问题能追，让模型下判断会飘。
3. **模型不在线也能用**：报告和问答自动降级到规则引擎版本，主流程不阻塞。

另外补了会话历史：每次比对自动存档，每条消息带时间戳，服务重启不丢，可搜索、恢复、删除。

**验证过**：单元测试 16 项全过；`scripts/` 下有四个自检脚本，能一键复现完整链路 —— 后端链路（`verify_pipeline.py`）、会话持久化（`verify_history_api.py`）、离线 Demo 的比对引擎（`verify_demo.cjs`）、离线 Demo 的交互流程（`verify_demo_ui.cjs`）。

## 二、几个刻意的取舍

- **比对用 difflib + 滑动窗口，没上向量检索**。版本演进里段落顺序基本是稳定的，滑窗够用，零额外依赖、结果可复现。真要处理跨章节大范围重排再上向量。
- **大模型只做表达，不做判断**（理由见上面第 2 点）。
- **Docling 设为可选依赖**，装不上自动走 pypdf。它要拉 torch 和一整套解析模型，不能因为它让整个项目跑不起来。
- **前端零构建**，原生三件套，打开就能跑。

## 三、我自己觉得还不够的地方

**解析层**
- Docling 本机装了几轮都失败（依赖链太重），**当前实际跑的是 pypdf 兜底路径**。结构化解析的代码写了，但我没真正验证过效果，标题层级和表格还原我不敢替它保证。
- 扫描件、图片型 PDF 处理不了，没有 OCR 兜底。

**比对层**
- **阈值（0.95 / 0.6）是我拍的经验值**，没有标注数据做调参，算不出准确率和召回率。这是我最不放心的一块。
- 段落被大范围重排、跨章节搬家会失配，判成一删一增。窗口只有 ±5。
- 表格没有单独处理，被当普通文本逐行比，单元格错位会让整段判成「修改」。

**报告层**
- 模型写的审阅意见没有评测机制，好坏只能靠人看，也没沉淀 Bad Case。
- 风险关键词表（金额、期限、违约这些）是我自己列的，没对齐过真实审查规范。

**工程层**
- 会话存 JSON 文件，单机单实例够用，多实例部署会有写冲突。
- 没有鉴权，没有上传大小和页数硬限制，大文档解析会阻塞请求（没上任务队列）。

## 四、想请您帮忙判断的几件事

1. **比对粒度上，偏「不漏」还是偏「不吵」？** 我现在偏召回，宁可多报也不漏改动，代价是偶尔有噪声。如果业务上更看重报告干净，判定策略要反过来调。
2. **实际场景里扫描件占多大比例？** 如果多数是扫描件和图片 PDF，我下一步该补 OCR，而不是继续优化文本比对。
3. **报告是终点还是中间产物？** 现在是给人看的 HTML / Markdown。如果下游要接系统（落库、触发审批流），我得把差异结果抽成一份稳定的结构化 schema。
4. **大模型的边界您更认哪种？** 我刻意只让它做表达。「模型自主判断」和「规则兜底 + 模型表达」，你们生产里走的是哪条？
5. **有没有我漏掉的场景？** 比如一份文档要同时对比多个历史版本、比对结果需要回流人工确认、或者权限与审计留痕这类硬要求。

---

就这些。哪里方向不对、或者有更该优先做的，麻烦您直接说，我按您的判断改。
